<%= render 'book_filter_table', title: 'All author with books', data: @all_authors_with_books %>

<%= render 'book_filter_table', title: 'Filter where authors zip_code is equal to 1100', data: @zip_code_filter if @zip_code_filter.length > 0 %>

<%= render 'book_filter_table', title: 'Filter where authors id is more than 20', data: @author_id_filter if @author_id_filter.length > 0 %>

<%= render 'book_filter_table', title: 'Filter where author has addresses', data: @author_address_filter if @author_address_filter.length > 0 %>

<%= render 'book_filter_table', title: 'Filter where authors zip_code is not null', data: @zip_code_exist if @zip_code_exist.length > 0 %>

<%= render 'book_filter_table', title: 'Filter where published date is more than 2012', data: @published_at_filter if @published_at_filter.length > 0 %>

<%= render 'book_filter_table', title: 'Filter A - Book has at least one feedback', data: @books_with_feedbacks if @books_with_feedbacks.length > 0 %>

<%= render 'good_feedback_books', title: 'Filter B - feedback score is more than 60', data: @books_with_good_feedbacks if @books_with_good_feedbacks.length > 0 %>

<%= render 'good_feedback_books', title: 'books whose feedbacker is gia', data: @gia_feedbacker if @gia_feedbacker.length > 0 %>

<%= render 'users_form', title: 'all users who has more than 1 contact in address_book', data: @users_with_many_contacts if @users_with_many_contacts.length > 0 %>


